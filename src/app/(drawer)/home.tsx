import { Text, View } from 'react-native';

const Home = () => {
	return (
		<View className="bg-slate-800 flex h-full w-full justify-center items-center">
			<Text className="text-3xl text-white">Home</Text>
		</View>
	);
};

export default Home;
